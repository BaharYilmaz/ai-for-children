
    public partial class Form1 : Form

    {
        string[] iyiCumle = { "güzel", "iyi" };
        string[] kotuCumle = { "kötü", "çirkin" };

        public Form1()
        {
            InitializeComponent();
        }

        private void Form1_Load(object sender, EventArgs e)
        {
            programiCalistir();

        }


        private void whenEvent()
        {
            string cumle = "";
            string sonuc = "";


            cumle = typeSentence("güzel");
            sonuc= ifControl("mutlu",cumle);
        }

        private string switchControl(string durum,string cumle)
        {
            string sonuc = "";

            if (durum == "mutlu")
            {
                for (int i = 0; i < iyiCumle.Length; i++)
                {
                    if (iyiCumle[i]==cumle)
                    {
                        sonuc = "mutlu";
                    }

                }
            }
            if (durum == "mutsuz")
            {
                for (int i = 0; i < kotuCumle.Length; i++)
                {
                    if (kotuCumle[i] == cumle)
                    {
                        sonuc = "mutsuz" +
                            "";
                    }

                }
            }
            return sonuc;
        }
        private string typeSentence(string question)
        {

            return question;
          
        }
        private string ifControl(string durum,string cumle)
        {
            string sonuc = "";
            if (durum=="mutlu")
            {
               sonuc= switchControl(durum,cumle);
            }
            else
            {
                sonuc=switchControl(durum,cumle);
            }
            return sonuc;
        }
        private void programiCalistir()
        {
            whenEvent();

        }
        private void label1_Click(object sender, EventArgs e)
        {

        }
    }